(Meta-instruction: `CLAUDE.md` is symlinked to `AGENTS.md` - read only one of them.)

Read `README.md` for an overview of the codebase, an index for `docs/`, and coding style preferences.

Refer to the appropriate documentation pages if you need to, but you don't have to for simple and standalone tasks. If your changes make information in the docs no longer valid, update the docs and inform me in bold letters at the end of the message.

Adhere to the code guidelines, but don't actually run `clang-format` (I will do this myself periodically).

Ask me questions proactively if you have questions.

Avoid over-complicating things; avoid using hacks or lots of lines of code to implement a simple function. If there's no standard/simple solution, ask me.