#include <Arduino.h>

// Firmware entry points required by the arduino-esp32 core (cores/esp32/
// main.cpp calls setup() once, then loop() forever). These are placeholders
// until the controller/serial_io/display/status modules are implemented.

void setup() {}

void loop() {}
